<div class="row">
    <div class="content-container col-center">
        <div class="user-info col-md-6 col-center">
            <h1 class="page-title"><%= @user.name %></h1>
            <% if current_user?(@user) %>
                <div class="email">
                    <%= @user.email %>
                    <%= link_to '編集', edit_user_path(@user.id) %>
                </div>
            <% end %>
            <div class="users-picture row col-center">
                <% @user.pictures.each do |picture| %>
                    <ul class="col-md-4">
                        <%= link_to picture_path(picture.id) do %>
                            <li><%= image_tag(picture.image.url) if picture.image.url %></li>
                        <% end %>
                    </ul>
                <% end %>
            </div>
        </div>
    </div>
</div>
